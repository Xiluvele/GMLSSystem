@model IEnumerable<GMLSSystem.Shared.Models.ContractDto>

@if (Model != null && Model.Any())
{
    <div class="table-responsive">
        <table class="table table-hover">
            <thead class="table-light">
                <tr>
                    <th>Contract #</th>
                    <th>Client</th>
                    <th>Start Date</th>
                    <th>End Date</th>
                    <th>Status</th>
                    <th>Service Level</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var contract in Model)
                {
                    <tr>
                        <td>
                            <strong>@contract.ContractNumber</strong>
                            @if (!string.IsNullOrEmpty(contract.SignedAgreementPath))
                            {
                                <br />
                                <a href="@contract.SignedAgreementPath" target="_blank" class="small text-danger">
                                    <i class="fas fa-file-pdf"></i> View PDF
                                </a>
                            }
                        </td>
                        <td>@contract.ClientName</td>
                        <td>@contract.StartDate.ToShortDateString()</td>
                        <td>@contract.EndDate.ToShortDateString()</td>
                        <td>
                            @{
                                var statusClass = contract.Status switch
                                {
                                    "Active" => "success",
                                    "Draft" => "warning",
                                    "Expired" => "danger",
                                    "OnHold" => "secondary",
                                    _ => "secondary"
                                };
                            }
                            <span class="badge bg-@statusClass">@contract.Status</span>
                        </td>
                        <td>
                            <span class="badge bg-info">@contract.ServiceLevel</span>
                        </td>
                        <td>
                            <a asp-action="Details" asp-route-id="@contract.ContractId" class="btn btn-sm btn-info">
                                <i class="fas fa-eye"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <div class="alert alert-info text-center">
        <i class="fas fa-info-circle"></i> No contracts found.
    </div>
}